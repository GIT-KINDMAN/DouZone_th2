알마인드 22.02.03 참고


// 서로 다른 것도 묶어서 표현하고 싶어! -> Structure 등장 배경 -> 그러나 누구든 변경 가능

// C++에서도 이 문제를 고치려 했으나 못했었음. 호환성 때문에

// 그래서 새로이 만들어진 자료 타입이 클래스

// 멤버 변수는 캡슐화 해주고, 멤버 함수를 통해 private 멤버 변수에 접근함

// Class 설계도
"의자" - 고유 특징 (속성)
        색상 // 멤버 변수
        모양
        재질

        기능 동작(함수)

        앉는다 {} // 멤버함수

        색상 변경 함수 {
            모양
            재질
        }


// Class 규칙

                        <<- void setX(int xx)
ㅡㅡㅡㅡㅡ A ㅡㅡㅡㅡㅡ                        ㅡㅡㅡㅡㅡ B ㅡㅡㅡㅡㅡ
int x, y;              <- 직접 교류 X ->       char ch = 'B';
x=5;                                          int a, b;
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ                       ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
                            int getX() ->>

A a = new A();

접근하기 위해 get 함수, set 함수 활용

int getX() {
    return x;
}


void setX(int xx) {
    
}